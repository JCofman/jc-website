---
path: '/make-vscode-better'
id: '/make-vscode-better'
date: '2022-05-03T14:12:33.962Z'
title: 'Make VS Code better by editing and updating some settings'
tags: [vscode, dx, ide]
featuredImage: './featured-image.jpg'
headerImage: './header.jpg'
excerpt:
  'This article shows some valuable tips and tricks on how to make
VS Code development experience a bit better (at least for my kind)'
---

import Kbd from '../../../src/components/Kbd';
import bracketColorizerVideo from './bracket-colorizer.mp4';
import bracketPairVideo from './bracket-pair-guides.mp4';
import fontLigatureVideo from './font-ligatures.mp4';
import highlightModifiedTab from './highlight-modified-tab.mp4';
import inlayHintsVideo from './inlay-hints.mp4';

# Make VS Code better by just editing some settings

This article shows some valuable tips and tricks on how to make
[VS Code](https://code.visualstudio.com/) development experience a bit better (at least for my kind
of use cases) by enabling and editing some settings which are not set by default. You can go to your
settings and enable each one of these by either opening it up via the command palette

1. <Kbd>CMD</Kbd> + <Kbd>⌘</Kbd> + <Kbd>P</Kbd>
2. Type in _settings_
3. Select _Open Workspace Settings (JSON)_

or with the shortcut (<Kbd>CMD</Kbd> + <Kbd>,</Kbd>) and click on the icon Open Settings (JSON) on
the upper right corner.

![Settings dialog in VS Code](./settings.png)

Settings dialog in VS Code

Here are the five 🔥 things I add to improve my development environment.

## 1. Enable bracket colorization

Using bracket colorization makes it easier to see where code blocks start and end.

<video controls style={{ width: `100%` }}>
  <source src={bracketColorizerVideo} type="video/mp4" />
</video>

```jsx
// settings.json

// 1. enables vscode native bracket colorization
 "editor.bracketPairColorization.enabled": true
```

## 2. Enable bracket pair guides

Besides using bracket colorization, I love to add bracket pair guides.

<video controls style={{ width: `100%` }}>
  <source src={bracketPairColorization} type="video/mp4" />
</video>

```jsx
// settings.json

// 1. enables vscode native bracket colorization
 "editor.bracketPairColorization.enabled": true
```

## 3. Highlight modified Tab

I like to make it more clear which file I work on.

<video controls style={{ width: `100%` }}>
  <source src={highlightModifiedTab} type="video/mp4" />
</video>

```jsx
// settings.json

// 2. highlight active tab
  "workbench.editor.highlightModifiedTabs": true,
```

## 4. Using Font Ligatures

It makes it easier to distinguish comparisons between code lines.

<video controls style={{ width: `100%` }}>
  <source src={fontLigatureVideo} type="video/mp4" />
</video>

```jsx
// use a font with font ligatures
	 "editor.fontFamily": "Jetbrains Mono, Menlo, Monaco, 'Courier New', monospace",
// 3. enable font ligature
	 "editor.fontLigatures": true,
```

## 5. Enabling Inlay hints

It can help you see what kind of properties needs to get passed into functions. without having to
hover over the function call.

<video controls style={{ width: `100%` }}>
  <source src={inlayHintsVideo} type="video/mp4" />
</video>
